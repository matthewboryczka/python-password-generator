# Python Password Generator

A simple Python script that generates a random password using letters, numbers, and symbols.

## How It Works

The script:
1. Creates a pool of characters (letters, numbers, symbols)
2. Randomly selects characters
3. Builds a password of a specified length

## How To Run

Run the script with:

python3 generator.py
